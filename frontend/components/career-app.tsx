'use client'

import { useMemo, useState } from 'react'
import { BriefcaseBusiness, Check, ChevronDown, FileText, FlaskConical, Gauge, Menu, Plus, Search, Settings2, Sparkles, UserRound, X } from 'lucide-react'
import { defaultPreferences, mockApplications, mockJobs, mockProfile } from '@/lib/types'
import type { Application, Job, Preferences, Profile } from '@/lib/types'

const nav = [{ id: 'profile', label: 'Profile', icon: UserRound }, { id: 'preferences', label: 'Preferences', icon: Settings2 }, { id: 'jobs', label: 'Find jobs', icon: Search }, { id: 'applications', label: 'Applications', icon: BriefcaseBusiness }, { id: 'dev', label: 'API test', icon: FlaskConical }]
const statuses: Application['status'][] = ['Saved', 'Applied', 'Interview', 'Offer']

function Button({ children, onClick, variant = 'primary', className = '', type = 'button' }: { children: React.ReactNode; onClick?: () => void; variant?: 'primary' | 'outline' | 'ghost'; className?: string; type?: 'button' | 'submit' }) { return <button type={type} onClick={onClick} className={`inline-flex items-center justify-center gap-2 rounded-lg px-3.5 py-2 text-sm font-semibold transition ${variant === 'primary' ? 'bg-[var(--ink)] text-white hover:bg-[var(--ink-soft)]' : variant === 'outline' ? 'border border-[var(--line)] bg-white text-[var(--ink)] hover:border-[var(--mint)]' : 'text-[var(--muted)] hover:bg-[var(--wash)]'} ${className}`}>{children}</button> }
function Badge({ children, tone = 'neutral' }: { children: React.ReactNode; tone?: 'neutral' | 'mint' | 'yellow' }) { return <span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${tone === 'mint' ? 'bg-[var(--mint-pale)] text-[var(--mint-deep)]' : tone === 'yellow' ? 'bg-[#fff3c4] text-[#785d00]' : 'bg-[var(--wash)] text-[var(--muted)]'}`}>{children}</span> }
function Card({ children, className = '' }: { children: React.ReactNode; className?: string }) { return <section className={`rounded-xl border border-[var(--line)] bg-white p-5 shadow-[0_2px_8px_rgba(24,42,50,.03)] ${className}`}>{children}</section> }
function Field({ label, value, onChange, placeholder, textarea = false }: { label: string; value: string; onChange: (v: string) => void; placeholder?: string; textarea?: boolean }) { return <label className="grid gap-1.5 text-sm font-semibold text-[var(--ink)]">{label}{textarea ? <textarea className="min-h-28 resize-y rounded-lg border border-[var(--line)] bg-[var(--paper)] px-3 py-2.5 font-normal outline-none transition focus:border-[var(--mint)] focus:ring-2 focus:ring-[var(--mint-pale)]" value={value} onChange={e => onChange(e.target.value)} placeholder={placeholder} /> : <input className="h-10 rounded-lg border border-[var(--line)] bg-[var(--paper)] px-3 font-normal outline-none transition focus:border-[var(--mint)] focus:ring-2 focus:ring-[var(--mint-pale)]" value={value} onChange={e => onChange(e.target.value)} placeholder={placeholder} />}</label> }

export default function CareerApp() {
  const [active, setActive] = useState('profile'); const [mobileOpen, setMobileOpen] = useState(false); const [profile, setProfile] = useState<Profile>(mockProfile); const [prefs, setPrefs] = useState<Preferences>(defaultPreferences); const [apps, setApps] = useState(mockApplications); const [selectedJob, setSelectedJob] = useState<Job | null>(null); const [query, setQuery] = useState(''); const [saved, setSaved] = useState(false)
  const go = (id: string) => { setActive(id); setMobileOpen(false); setSelectedJob(null) }
  const filteredJobs = useMemo(() => mockJobs.filter(j => `${j.title} ${j.company} ${j.tags.join(' ')}`.toLowerCase().includes(query.toLowerCase())), [query])
  return <div className="min-h-screen bg-[var(--paper)] text-[var(--ink)]"><aside className={`fixed inset-y-0 left-0 z-30 flex w-64 flex-col border-r border-[var(--line)] bg-white p-4 transition-transform lg:translate-x-0 ${mobileOpen ? 'translate-x-0' : '-translate-x-full'}`}><div className="flex items-center justify-between px-2 py-3"><div className="flex items-center gap-2"><span className="grid size-8 place-items-center rounded-lg bg-[var(--mint)] text-[var(--ink)]"><Sparkles size={17}/></span><span className="font-serif text-lg font-bold">career<span className="text-[var(--mint-deep)]">mate</span></span></div><Button variant="ghost" className="lg:hidden" onClick={() => setMobileOpen(false)}><X size={18}/></Button></div><div className="mt-8 grid gap-1">{nav.map(item => <button key={item.id} onClick={() => go(item.id)} className={`flex items-center gap-3 rounded-lg px-3 py-2.5 text-left text-sm font-semibold transition ${active === item.id ? 'bg-[var(--mint-pale)] text-[var(--mint-deep)]' : 'text-[var(--muted)] hover:bg-[var(--wash)]'}`}><item.icon size={17}/>{item.label}{item.id === 'jobs' && <span className="ml-auto rounded-full bg-[var(--ink)] px-1.5 text-[10px] text-white">12</span>}</button>)}</div><div className="mt-auto rounded-xl bg-[var(--ink)] p-4 text-white"><p className="text-xs font-semibold uppercase tracking-[.14em] text-[var(--mint)]">Profile strength</p><div className="mt-3 h-1.5 overflow-hidden rounded-full bg-white/20"><div className="h-full w-[78%] rounded-full bg-[var(--mint)]"/></div><p className="mt-2 text-sm">78% complete</p><button onClick={() => go('profile')} className="mt-3 text-xs font-semibold text-[var(--mint)]">Improve profile →</button></div></aside><div className="lg:pl-64"><header className="sticky top-0 z-20 flex h-16 items-center justify-between border-b border-[var(--line)] bg-[var(--paper)]/95 px-5 backdrop-blur lg:px-8"><div className="flex items-center gap-3"><Button variant="ghost" className="lg:hidden" onClick={() => setMobileOpen(true)}><Menu size={20}/></Button><div><p className="text-xs font-semibold uppercase tracking-[.14em] text-[var(--muted)]">Workspace</p><h1 className="font-serif text-xl font-bold">{nav.find(n => n.id === active)?.label}</h1></div></div><div className="flex items-center gap-3"><span className="hidden text-xs text-[var(--muted)] sm:block">API: <b className="text-[var(--mint-deep)]">mock mode</b></span><span className="grid size-8 place-items-center rounded-full bg-[#d9edf1] text-xs font-bold">MC</span></div></header><main className="mx-auto max-w-6xl p-5 lg:p-8">{active === 'profile' && <ProfileView profile={profile} setProfile={setProfile} saved={saved} setSaved={setSaved}/>} {active === 'preferences' && <PreferencesView prefs={prefs} setPrefs={setPrefs}/>} {active === 'jobs' && <JobsView jobs={filteredJobs} query={query} setQuery={setQuery} onSelect={setSelectedJob} selected={selectedJob} onSave={() => setApps(a => [...a, { id: `a${a.length+1}`, title: selectedJob?.title || '', company: selectedJob?.company || '', status: 'Saved', date: 'Today', match: selectedJob?.match || 0 }])}/>} {active === 'applications' && <ApplicationsView apps={apps} setApps={setApps}/>} {active === 'dev' && <DevView/>}</main></div></div>
}

function ProfileView({ profile, setProfile, saved, setSaved }: { profile: Profile; setProfile: React.Dispatch<React.SetStateAction<Profile>>; saved: boolean; setSaved: (v: boolean) => void }) { const [file, setFile] = useState(''); const update = (key: keyof Profile, value: string) => setProfile(p => ({ ...p, [key]: value })); return <div className="grid gap-6"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow">Your career context</p><h2 className="mt-1 font-serif text-3xl font-bold tracking-tight">A profile that sounds like you.</h2><p className="mt-2 max-w-xl text-sm leading-6 text-[var(--muted)]">Import your background once. career mate uses it as the source of truth for every recommendation and application.</p></div><Button onClick={() => setSaved(true)}><Check size={16}/>{saved ? 'Saved' : 'Save changes'}</Button></div><div className="grid gap-6 xl:grid-cols-[.8fr_1.2fr]"><Card><div className="flex items-center justify-between"><div><h3 className="font-serif text-lg font-bold">Import profile</h3><p className="mt-1 text-sm text-[var(--muted)]">CVs and supporting files</p></div><FileText className="text-[var(--mint-deep)]"/></div><label className="mt-5 grid min-h-44 cursor-pointer place-items-center rounded-xl border border-dashed border-[var(--line-strong)] bg-[var(--paper)] p-6 text-center hover:border-[var(--mint-deep)]"><input className="sr-only" type="file" accept=".pdf,.doc,.docx" onChange={e => setFile(e.target.files?.[0]?.name || '')}/><div><span className="mx-auto grid size-10 place-items-center rounded-full bg-[var(--mint-pale)]"><Plus size={18}/></span><p className="mt-3 text-sm font-semibold">{file || 'Drop a CV here or browse'}</p><p className="mt-1 text-xs text-[var(--muted)]">PDF or DOCX, up to 10MB</p></div></label><div className="mt-4 grid gap-3"><Field label="LinkedIn URL" value="https://linkedin.com/in/mayachen" onChange={() => {}}/><Field label="Portfolio URL" value="https://mayachen.design" onChange={() => {}}/></div><Button className="mt-4 w-full" onClick={() => setSaved(true)}><Sparkles size={16}/>Analyze profile</Button></Card><Card><div className="flex items-center justify-between"><div><h3 className="font-serif text-lg font-bold">Structured profile</h3><p className="mt-1 text-sm text-[var(--muted)]">Edit anything that needs a human touch.</p></div><Badge tone="mint">Ready to use</Badge></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><Field label="Name" value={profile.name} onChange={v => update('name', v)}/><Field label="Location" value={profile.location} onChange={v => update('location', v)}/></div><div className="mt-4 grid gap-4"><Field label="Headline" value={profile.headline} onChange={v => update('headline', v)}/><Field label="Summary" value={profile.summary} onChange={v => update('summary', v)} textarea/></div><div className="mt-5"><p className="text-sm font-semibold">Skills</p><div className="mt-2 flex flex-wrap gap-2">{profile.skills.map(skill => <Badge key={skill} tone="mint">{skill}</Badge>)}<button className="rounded-full border border-dashed border-[var(--line-strong)] px-2.5 py-1 text-xs font-semibold text-[var(--muted)]"><Plus size={12} className="mr-1 inline"/>Add skill</button></div></div></Card></div></div> }

function PreferencesView({ prefs, setPrefs }: { prefs: Preferences; setPrefs: React.Dispatch<React.SetStateAction<Preferences>> }) { return <div className="grid gap-6"><div><p className="eyebrow">Personalize your search</p><h2 className="mt-1 font-serif text-3xl font-bold">Tell us what good looks like.</h2><p className="mt-2 text-sm text-[var(--muted)]">These preferences tune your matches. You can change them anytime.</p></div><Card className="max-w-3xl"><div className="grid gap-5 sm:grid-cols-2"><Field label="Target titles" value={prefs.titles} onChange={v => setPrefs({...prefs, titles: v})}/><Field label="Locations" value={prefs.locations} onChange={v => setPrefs({...prefs, locations: v})}/><Field label="Salary range" value={prefs.salary} onChange={v => setPrefs({...prefs, salary: v})}/><Field label="Industries" value={prefs.industries} onChange={v => setPrefs({...prefs, industries: v})}/></div><label className="mt-5 flex cursor-pointer items-center justify-between rounded-lg bg-[var(--paper)] p-4"><span><span className="block text-sm font-semibold">Open to remote roles</span><span className="text-xs text-[var(--muted)]">Include fully distributed teams in your search.</span></span><input type="checkbox" checked={prefs.remote} onChange={e => setPrefs({...prefs, remote: e.target.checked})} className="size-5 accent-[var(--mint-deep)]"/></label><Button className="mt-6">Save preferences</Button></Card></div> }

function JobsView({ jobs, query, setQuery, onSelect, selected, onSave }: { jobs: Job[]; query: string; setQuery: (v: string) => void; onSelect: (j: Job) => void; selected: Job | null; onSave: () => void }) { return <div className="grid gap-6"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow">Based on your profile</p><h2 className="mt-1 font-serif text-3xl font-bold">Roles worth your attention.</h2></div><div className="relative"><Search className="absolute left-3 top-2.5 text-[var(--muted)]" size={16}/><input value={query} onChange={e => setQuery(e.target.value)} placeholder="Search jobs" className="h-10 w-56 rounded-lg border border-[var(--line)] bg-white pl-9 pr-3 text-sm outline-none focus:border-[var(--mint)]"/></div></div><div className="grid gap-4 xl:grid-cols-2">{jobs.map(job => <button key={job.id} onClick={() => onSelect(job)} className={`text-left ${selected?.id === job.id ? 'ring-2 ring-[var(--mint)]' : ''}`}><Card className="h-full transition hover:-translate-y-0.5 hover:border-[var(--mint)]"><div className="flex items-start justify-between gap-3"><div><p className="text-sm font-bold">{job.company}</p><h3 className="mt-1 font-serif text-xl font-bold">{job.title}</h3></div><Badge tone="mint">{job.match}% match</Badge></div><p className="mt-3 text-sm leading-6 text-[var(--muted)]">{job.description}</p><div className="mt-4 flex flex-wrap gap-2">{job.tags.map(t => <Badge key={t}>{t}</Badge>)}</div><div className="mt-5 flex items-center justify-between border-t border-[var(--line)] pt-4 text-xs text-[var(--muted)]"><span>{job.location} · {job.mode}</span><span className="font-semibold text-[var(--ink)]">{job.salary}</span></div></Card></button>)}</div>{selected && <Card className="border-[var(--mint)] bg-[var(--mint-pale)]"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="eyebrow">Selected role</p><h3 className="font-serif text-2xl font-bold">{selected.title} at {selected.company}</h3><p className="mt-1 text-sm text-[var(--muted)]">Strong fit across product thinking, systems, and collaboration.</p></div><div className="flex gap-2"><Button variant="outline" onClick={onSave}>Save role</Button><Button>Generate application <ChevronDown size={15}/></Button></div></div></Card>}</div> }

function ApplicationsView({ apps, setApps }: { apps: Application[]; setApps: React.Dispatch<React.SetStateAction<Application[]>> }) { return <div className="grid gap-6"><div className="flex items-end justify-between"><div><p className="eyebrow">Your pipeline</p><h2 className="mt-1 font-serif text-3xl font-bold">Keep the momentum visible.</h2></div><Button><Plus size={16}/>Add application</Button></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{statuses.map(status => <div key={status} className="min-h-56 rounded-xl border border-[var(--line)] bg-[var(--wash)] p-3"><div className="flex items-center justify-between px-1"><h3 className="text-sm font-bold">{status}</h3><span className="text-xs text-[var(--muted)]">{apps.filter(a => a.status === status).length}</span></div><div className="mt-3 grid gap-3">{apps.filter(a => a.status === status).map(app => <Card key={app.id} className="p-4"><div className="flex items-start justify-between gap-2"><div><p className="text-xs font-semibold text-[var(--muted)]">{app.company}</p><p className="mt-1 text-sm font-bold">{app.title}</p></div><Badge tone="mint">{app.match}%</Badge></div><div className="mt-4 flex items-center justify-between text-xs text-[var(--muted)]"><span>{app.date}</span><select aria-label={`Change status for ${app.title}`} value={app.status} onChange={e => setApps(xs => xs.map(x => x.id === app.id ? {...x, status: e.target.value as Application['status']} : x))} className="bg-transparent font-semibold text-[var(--ink)]"><option>Saved</option><option>Applied</option><option>Interview</option><option>Offer</option></select></div></Card>)}</div></div>)}</div></div> }

function DevView() { const [method, setMethod] = useState('GET'); const [url, setUrl] = useState('/health'); const [body, setBody] = useState('{\n  "limit": 10\n}'); const [result, setResult] = useState('Run a request to inspect the configured API.'); return <div className="grid gap-6"><div><p className="eyebrow">For development</p><h2 className="mt-1 font-serif text-3xl font-bold">See the API contract in motion.</h2><p className="mt-2 text-sm text-[var(--muted)]">A small request console for connecting the Go backend without guessing.</p></div><Card className="max-w-4xl"><div className="grid gap-3 sm:grid-cols-[120px_1fr_auto]"><select value={method} onChange={e => setMethod(e.target.value)} className="h-10 rounded-lg border border-[var(--line)] bg-[var(--paper)] px-3 text-sm font-bold"><option>GET</option><option>POST</option><option>PUT</option><option>DELETE</option></select><input value={url} onChange={e => setUrl(e.target.value)} className="h-10 rounded-lg border border-[var(--line)] bg-[var(--paper)] px-3 text-sm"/><Button onClick={() => setResult(JSON.stringify({ ok: true, status: 200, endpoint: url, method }, null, 2))}>Send request</Button></div><div className="mt-5 grid gap-4 md:grid-cols-2"><label className="grid gap-2 text-sm font-semibold">Request body<textarea value={body} onChange={e => setBody(e.target.value)} className="min-h-44 rounded-lg border border-[var(--line)] bg-[var(--paper)] p-3 font-mono text-xs"/></label><div><p className="text-sm font-semibold">Response</p><pre className="mt-2 min-h-44 overflow-auto rounded-lg bg-[var(--ink)] p-3 font-mono text-xs leading-5 text-[var(--mint)]">{result}</pre></div></div></Card></div> }
